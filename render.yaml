services:
  - type: web
    name: pbl-manager-v031
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /student
    envVars:
      - key: PBL_ADMIN_PASSWORD
        sync: false
